describe('Object representing the pile of cards, empty or filled', function () {
	"use strict";
	var deck;

	beforeEach(function () {
		deck = new Deck();
	});

	it('should pass test if card is a ace, and stack is empty', function () {
		// var stack = new Stack('#js-stack', deck);
		// var card = deck['tiles-A'];
		// var test = function (interior, exterior) {
		// 	return typeof interior === undefined && Card.isAce(exterior);
		// }
		// stack.addFilter(test);
		// expect(stack.test(card.$element)).toBe(true);
	});
});
